/**
 * POST /api/onboarding (Phase 1 stub)
 *
 * Placeholder during schema migration.
 * Phase 2 will rebuild with proper step-by-step handling.
 */

import { NextResponse } from "next/server";

export async function POST() {
  return NextResponse.json(
    { error: "Phase 1: Onboarding API rebuild in Phase 2" },
    { status: 503 }
  );
}
