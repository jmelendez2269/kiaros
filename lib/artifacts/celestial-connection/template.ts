import { CONNECTION_RELATIONSHIP_LABELS, CelestialConnectionContractError, type CelestialConnectionArtifact, type CelestialConnectionPaperSize, type ConnectionNarrativeBlock } from "./contract.ts";
import { renderConnectionMapSvg } from "./connection-map-template.ts";

function esc(value: string): string { return value.replace(/[&<>"']/g, (char) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" })[char] ?? char); }
function page(content: string, className = ""): string { return `<section class="page ${className}">${content}<footer><span>KAIROS · CELESTIAL CONNECTION</span><span>✦</span></footer></section>`; }
function prose(block: ConnectionNarrativeBlock): string { return `<div class="chapter-head"><p class="eyebrow">Relationship field</p><h2>${esc(block.title)}</h2><p class="summary">${esc(block.summary)}</p></div><div class="prose">${block.paragraphs.map((text) => `<p>${esc(text)}</p>`).join("")}</div><p class="practice-title">Three practices</p><ol class="anchors">${block.anchors.map((text, index) => `<li><span>0${index + 1}</span><p>${esc(text)}</p></li>`).join("")}</ol>`; }
function placementTable(title: string, items: CelestialConnectionArtifact["calculation"]["personAPlacements"]): string { return `<div class="table-card"><h3>${esc(title)}</h3><table><thead><tr><th>Point</th><th>Placement</th><th>House</th></tr></thead><tbody>${items.map((item) => `<tr><td>${esc(item.point)}</td><td>${item.degree.toFixed(1)}° ${esc(item.sign)}${item.timeSensitive ? "*" : ""}</td><td>${item.house ?? "—"}</td></tr>`).join("")}</tbody></table></div>`; }
export function renderCelestialConnectionReportHtml(artifact: CelestialConnectionArtifact, paperSize: CelestialConnectionPaperSize): string {
  if (!artifact.narrative) throw new CelestialConnectionContractError("Relationship report HTML requires an approved narrative.");
  const width = paperSize === "letter" ? 215.9 : 210; const height = paperSize === "letter" ? 279.4 : 297;
  const a = esc(artifact.input.personA.displayName); const b = esc(artifact.input.personB.displayName); const n = artifact.narrative;
  const topThree = artifact.calculation.signatures.map((item) => `<li><span>0${item.rank}</span><div><strong>${esc(item.title)}</strong><p>${esc(`${artifact.input.personA.displayName} ${item.personAPoint} ${item.aspect} ${artifact.input.personB.displayName} ${item.personBPoint}`)} · ${item.orb.toFixed(2)}° orb</p></div></li>`).join("");
  const signaturePages = n.signatures.map((block, index) => { const fact = artifact.calculation.signatures[index]; return page(`<div class="rank">0${index + 1}</div><div class="chapter-head"><p class="eyebrow">Top relationship signature</p><h2>${esc(block.title)}</h2><p class="summary">${esc(block.summary)}</p><div class="fact-band">${esc(`${artifact.input.personA.displayName}’s ${fact.personAPoint} ${fact.aspect} ${artifact.input.personB.displayName}’s ${fact.personBPoint}`)} · ${fact.orb.toFixed(2)}° orb · ${esc(fact.tone.replace(/_/g, " "))}</div></div><div class="prose">${block.paragraphs.map((text) => `<p>${esc(text)}</p>`).join("")}</div><blockquote>${esc(block.repairPractice)}</blockquote><p class="practice-title">Ways to work with it</p><ol class="anchors">${block.anchors.map((text, i) => `<li><span>0${i + 1}</span><p>${esc(text)}</p></li>`).join("")}</ol>`, "signature-page"); }).join("");
  const domainPages = n.domains.map((block) => page(`<p class="eyebrow">${esc(block.id.replace(/_/g, " "))}</p>${prose(block)}`, "domain-page")).join("");
  const html = `<!doctype html><html lang="en"><head><meta charset="utf-8"><title>Celestial Connection · ${a} and ${b}</title><style>
  @page{size:${width}mm ${height}mm;margin:0}*{box-sizing:border-box}html,body{margin:0;background:#151020;color:#211b2c;font-family:Georgia,'Times New Roman',serif}body{print-color-adjust:exact;-webkit-print-color-adjust:exact}.page{position:relative;width:${width}mm;height:${height}mm;overflow:hidden;background:#f5efe4;padding:17mm 17mm 15mm;page-break-after:always}.page:last-child{page-break-after:auto}.page:before{content:"";position:absolute;inset:6mm;border:1px solid #b89b72;pointer-events:none}.page footer{position:absolute;left:17mm;right:17mm;bottom:8.5mm;display:flex;justify-content:space-between;color:#745f54;font:700 7pt Arial;letter-spacing:.16em}.cover{background:radial-gradient(circle at 50% 30%,#563b69 0,#281a39 42%,#110d1a 84%);color:#fff9ed;padding:0}.cover:before{border-color:#c5a271}.cover-inner{position:absolute;inset:18mm;display:flex;flex-direction:column;justify-content:center;text-align:center;border:1px solid #a98255;padding:12mm}.rings{width:50mm;height:50mm;margin:0 auto 11mm;position:relative;border:1px solid #d79c72;border-radius:50%;box-shadow:0 0 0 6mm #a98acb22,0 0 30mm #7f55a64d}.rings:before,.rings:after{content:"";position:absolute;border-radius:50%;inset:7mm;border:1px solid #a98acb}.rings:after{inset:18mm;background:#d4ae73}.kicker,.eyebrow{font:800 8pt Arial;letter-spacing:.22em;text-transform:uppercase;color:#9e5d3c}.cover .kicker{color:#efc58c}.cover h1{font-size:37pt;line-height:1.02;margin:0}.cover h2{font-size:18pt;font-weight:400;font-style:italic;color:#decfe7;margin:4mm 0 8mm}.names{font:700 12pt Arial;letter-spacing:.12em;text-transform:uppercase}.cover .relation{font:8pt Arial;color:#d7c7df;margin-top:3mm;letter-spacing:.15em}.intro h2,.reference h2,.reflection h2,.overview h2{font-size:29pt;color:#302345;margin:4mm 0 6mm}.letter{font-size:11.7pt;line-height:1.7}.letter p:first-child:first-letter{float:left;font-size:42pt;line-height:.8;color:#a6603c;padding-right:2mm}.chapter-head{border-left:2.2mm solid #b76742;background:#302345;color:#fff9ef;padding:7mm 8mm;margin-bottom:7mm}.chapter-head h2{font-size:26pt;line-height:1.08;margin:2mm 0}.chapter-head .eyebrow{color:#efc68e}.summary{font-size:11.3pt;line-height:1.5;font-weight:700;margin:2mm 0 0}.prose{font-size:10.5pt;line-height:1.62;columns:2;column-gap:9mm}.prose p{margin:0 0 5mm}.practice-title{font:800 8pt Arial;letter-spacing:.18em;text-transform:uppercase;color:#684a8d;margin:6mm 0 3mm}.anchors{list-style:none;padding:0;margin:0;display:grid;gap:2.5mm}.anchors li{display:grid;grid-template-columns:10mm 1fr;border-top:1px solid #c5b19a;padding-top:2.5mm}.anchors span{font:800 8pt Arial;color:#a45f3b}.anchors p{margin:0;font:9pt/1.45 Arial}.rank{position:absolute;right:17mm;top:12mm;font:700 40pt Arial;color:#c26d4220}.fact-band{margin:4mm -2mm -2mm;padding:3mm 4mm;background:#f0d2b2;color:#302345;font:700 8.5pt Arial}blockquote{margin:5mm 0;padding:4mm 5mm;border-left:1.5mm solid #785695;background:#e8dff0;font-size:10.5pt;font-style:italic;line-height:1.5}.tables{display:grid;grid-template-columns:1fr 1fr;gap:5mm}.table-card{border:1px solid #b89b72;padding:4mm}.table-card h3{font-size:13pt;margin:0 0 3mm;color:#4c3769}.table-card table{width:100%;border-collapse:collapse;font:7.7pt Arial}.table-card th,.table-card td{padding:1.25mm;border-bottom:1px solid #d7c9b8;text-align:left}.method{margin-top:5mm;padding:4mm;background:#302345;color:#fff;font:8.4pt/1.5 Arial}.map-grid{display:grid;grid-template-columns:1.1fr .9fr;gap:7mm;align-items:center}.chart svg{width:100%;height:auto}.outer,.orbit-a,.orbit-b{fill:none}.outer{stroke:#8d719e;stroke-width:.4}.orbit-a{stroke:#b8613d;stroke-width:.55}.orbit-b{stroke:#7c5c9f;stroke-width:.55}.tick{stroke:#a993b5;stroke-width:.3}.person-a{fill:#b8613d;stroke:#fff;stroke-width:.4}.person-b{fill:#7c5c9f;stroke:#fff;stroke-width:.4}.point-label{fill:#21182c;font:700 1.45px Arial;text-anchor:middle}.sig{fill:none;stroke-width:.6}.sig-1{stroke:#a85a35}.sig-2{stroke:#79559a;stroke-dasharray:2 1}.sig-3{stroke:#39738f;stroke-dasharray:.7 1}.rank-bg{fill:#302345;stroke:#fff;stroke-width:.25}.rank{fill:#fff;font:700 1.8px Arial;text-anchor:middle}.core{fill:#302345;stroke:#b89b72;stroke-width:.4}.core-kicker,.core-title,.core-note{fill:#fff;text-anchor:middle;font-family:Arial}.core-kicker{font-size:1.5px}.core-title{font-size:2px;font-weight:700}.core-note{font-size:1.1px}.top-three{list-style:none;padding:0;margin:0;display:grid;gap:3mm}.top-three li{display:grid;grid-template-columns:12mm 1fr;gap:3mm;border-left:2mm solid #a6603c;background:#ece1d3;padding:4mm}.top-three span{font:800 9pt Arial;color:#9d5937}.top-three strong{font-size:11pt}.top-three p{margin:1mm 0 0;font:8.3pt Arial;color:#5b5058}.composite-list{columns:2;list-style:none;padding:0}.composite-list li{break-inside:avoid;border-bottom:1px solid #c9b69e;padding:2.2mm;font:9.5pt Arial}.reflection ol{columns:2;column-gap:10mm;padding-left:7mm;font-size:10.8pt;line-height:1.55}.reflection li{break-inside:avoid;margin-bottom:5mm}.scope{margin-top:6mm;border-top:1px solid #b89b72;padding-top:4mm;font:8pt/1.5 Arial;color:#5c5159}
  .signature-page>.rank{position:absolute;right:17mm;top:12mm;font:700 40pt Arial;color:#c26d4220;text-anchor:initial}
  </style></head><body><main aria-label="Celestial Connection relationship report">
  ${page(`<div class="cover-inner"><div class="rings"></div><p class="kicker">Kairos · Relationship astrology</p><h1>Celestial Connection</h1><h2>Synastry & Composite Report</h2><p class="names">${a} &amp; ${b}</p><p class="relation">${esc(CONNECTION_RELATIONSHIP_LABELS[artifact.input.relationshipType])}</p></div>`, "cover")}
  ${page(`<div class="intro"><p class="eyebrow">A letter to the connection</p><h2>Two whole skies,<br>one living conversation</h2><div class="letter">${n.openingLetter.map((text) => `<p>${esc(text)}</p>`).join("")}</div><div class="method"><strong>Reading stance</strong> This map holds resonance and friction together. It is not a score, verdict, prediction, or substitute for direct communication.</div></div>`, "intro")}
  ${page(`<div class="reference"><p class="eyebrow">Astronomical foundation</p><h2>The two natal references</h2><div class="tables">${placementTable(artifact.input.personA.displayName, artifact.calculation.personAPlacements)}${placementTable(artifact.input.personB.displayName, artifact.calculation.personBPlacements)}</div><div class="method"><strong>Method</strong> Tropical zodiac · Whole Sign houses · major synastry aspects · short-arc composite midpoints · astronomia 4.2.0<br><strong>Precision</strong> ${esc(artifact.calculation.precision.note)}</div></div>`, "reference")}
  ${page(`<div class="overview"><p class="eyebrow">The relationship at a glance</p><h2>Your Top 3 signatures</h2><div class="map-grid"><div class="chart">${renderConnectionMapSvg(artifact)}</div><ol class="top-three">${topThree}</ol></div><div class="method">Numbers, line patterns, planet names, aspect names, and exact orbs repeat the visual meaning; color is never the only cue.</div></div>`, "overview")}
  ${page(prose(n.relationshipEssence), "essence")}${signaturePages}${domainPages}
  ${page(`<div class="chapter-head"><p class="eyebrow">Composite center</p><h2>${esc(n.compositeCore.title)}</h2><p class="summary">${esc(n.compositeCore.summary)}</p></div><ul class="composite-list">${artifact.calculation.compositePlacements.map((item) => `<li><strong>${esc(item.point)}</strong> · ${item.degree.toFixed(1)}° ${esc(item.sign)}</li>`).join("")}</ul><div class="prose">${n.compositeCore.paragraphs.map((text) => `<p>${esc(text)}</p>`).join("")}</div><p class="practice-title">Shared anchors</p><ol class="anchors">${n.compositeCore.anchors.map((text, i) => `<li><span>0${i + 1}</span><p>${esc(text)}</p></li>`).join("")}</ol>`, "composite")}
  ${page(`<div class="reflection"><p class="eyebrow">Integration</p><h2>Questions for the relationship</h2><ol>${n.reflectionPrompts.map((text) => `<li>${esc(text)}</li>`).join("")}</ol><div class="scope"><strong>Scope and care.</strong> ${esc(artifact.scopeNote)}<br><br><strong>AI disclosure.</strong> Interpretive prose was drafted with AI from the calculated facts shown in this report, then held for human review before export. Birth details are private fulfillment data. No compatibility percentage or guaranteed outcome is produced.</div></div>`, "reflection")}
  </main></body></html>`;
  return html;
}
