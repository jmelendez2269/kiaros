/**
 * Blueprint generation service (Phase 1 stub)
 *
 * Placeholder during schema migration to Phase 3 real implementation.
 * Phase 3 will integrate ephemeris + AI SDK v6 for real blueprint generation.
 */

/**
 * Stub: Phase 3 will implement real blueprint generation with ephemeris.
 */
export async function generateBlueprintContent(
  _onboarding: unknown,
  _userName: string
): Promise<Record<string, unknown>> {
  throw new Error("Phase 1 stub: Blueprint generation in Phase 3");
}
